name: Deploy to VPS

on:
    push:
        branches: [main]

env:
    REGISTRY: ghcr.io

jobs:
    build-and-push:
        runs-on: ubuntu-latest
        permissions:
            contents: read
            packages: write

        steps:
            - name: Checkout repository
              uses: actions/checkout@v2

            - name: Log in to the Container registry
              uses: docker/login-action@v2
              with:
                  registry: ${{ env.REGISTRY }}
                  username: ${{ github.actor }}
                  password: ${{ secrets.GITHUB_TOKEN }}

            - name: Convert repository name to lowercase
              id: repo-name
              run: echo "REPO_NAME=$(echo ${{ github.repository }} | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT

            - name: Build and push Docker image
              uses: docker/build-push-action@v3
              with:
                  context: .
                  push: true
                  tags: ${{ env.REGISTRY }}/${{ steps.repo-name.outputs.REPO_NAME }}:latest

    deploy:
        needs: build-and-push
        runs-on: ubuntu-latest

        steps:
            - name: Convert repository name to lowercase
              id: repo-name
              run: echo "REPO_NAME=$(echo ${{ github.repository }} | tr '[:upper:]' '[:lower:]')" >> $GITHUB_OUTPUT

            - name: Deploy to VPS
              uses: appleboy/ssh-action@master
              with:
                  host: ${{ secrets.HOST }}
                  username: ${{ secrets.USERNAME }}
                  key: ${{ secrets.SSH_PRIVATE_KEY }}
                  script: |
                      docker pull ${{ env.REGISTRY }}/${{ steps.repo-name.outputs.REPO_NAME }}:latest
                      docker stop zentodo || true
                      docker rm zentodo || true
                      docker run -d --name zentodo --network app_network -p 3001:3001 ${{ env.REGISTRY }}/${{ steps.repo-name.outputs.REPO_NAME }}:latest
